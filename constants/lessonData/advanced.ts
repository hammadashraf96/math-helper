import type { LessonContent } from '../../services/lessons';

// Advanced lessons — AI generation handles these at runtime (premium)
export const ADVANCED_LESSONS: Record<string, LessonContent> = {};
